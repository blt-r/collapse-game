import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "app.netlify.collapse_game.collapse",
  appName: "collapse-game",
  webDir: "build",
};

export default config;
